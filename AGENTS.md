# 项目协作说明

本项目中的所有说明、注释、文档、提交说明与协作沟通，默认使用简体中文。

## 技术栈要求

### 前端

- 前端统一使用 React.js 实现。
- 新增前端页面、组件、交互逻辑时，必须基于 React.js 及其生态完成，不使用 Vue、Angular、Svelte 等其他前端框架替代。
- 前端代码应保持组件化，避免将大量业务逻辑直接堆积在单个页面文件中。

### 后端

- 后端统一使用 Go 语言开发。
- Go 版本固定为 1.25。
- 新增服务、接口、任务或中间件时，必须兼容 Go 1.25，不得依赖更高版本特性。
### 数据库

- 数据库统一使用postgreSQL 17


### API 网关

- API 代理网关统一使用 Envoy AI Gateway。
- 所有面向外部或需要统一治理的 API 入口，应优先通过 Envoy AI Gateway 暴露和编排。
- 涉及鉴权、路由、流量治理、模型代理或统一观测时，应优先在 Envoy AI Gateway 层设计，而不是在业务代码中重复实现。

## 实施约束

- 进行前端开发时，默认按 React.js 项目结构组织代码。
- 进行后端开发时，默认按 Go 1.25 工程方式组织代码、依赖与构建流程。
- 涉及前后端联调的接口设计，应优先保证网关、后端接口、前端调用三者职责清晰。
- 未经明确说明，不随意引入与上述技术栈冲突的替代方案。

## 变更原则

- 如果需求与本文件约束冲突，先更新本文件，再实施代码变更。
- 如需引入新的基础设施或框架，必须先说明原因、影响范围与替代成本，再决定是否采用。
